<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="wiki.laona.mapper.ItemsCommentsMapperCustom">
	
	<insert id="saveComments" parameterType="map">
		insert into items_comments(
				id,
				user_id,
				item_id,
				item_name,
				item_spec_id,
				spec_name,
				comment_level,
				content,
				created_time,
				updated_time
		) values
			<foreach collection="paramsMap.commentList" item="item" index="index" separator=",">
				(
					#{item.commentId},
					#{paramsMap.userId},
					#{item.itemId},
					#{item.itemName},
					#{item.itemSpecId},
					#{item.itemSpecName},
					#{item.commentLevel},
					#{item.content},
					now(),
					now()
				)
			</foreach>
	</insert>
	<!--
	    private String commentId;
    private String content;
    private Date createdTime;
    private String itemId;
    private String itemName;
    private String specName;
    private String itemImg;
	-->
	<resultMap id="myComments" type="wiki.laona.pojo.vo.MyCommentVO">
		<id column="commentId" property="commentId"/>
		<result column="content" property="content"/>
		<result column="createdTime" property="createdTime"/>
		<result column="itemId" property="itemId"/>
		<result column="itemName" property="itemName"/>
		<result column="specName" property="specName"/>
		<result column="itemImg" property="itemImg"/>
	</resultMap>
	
	<select id="queryMyComments" parameterType="map" resultMap="myComments">
		select ic.id           as commentId,
		       ic.content      as content,
		       ic.created_time as createTime,
		       ic.item_id      as itemId,
		       ic.item_name    as itemName,
		       ic.spec_name    as specName,
		       ii.url          as itemImg
		from items_comments ic
			     left JOIN items_img ii
			               on ic.item_id = ii.item_id
		WHERE ic.user_id = #{paramsMap.userId}
		  and ii.is_main = 1
	
	</select>
</mapper>